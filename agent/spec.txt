Project: Car Inventory Manager

Context: The boilerplate already contains the completed logic files: `App.tsx`, `useCars.ts`, and `queries.ts`. DO NOT EDIT OR CREATE THOSE FILES. 

Task: Your ONLY job is to generate the frontend UI components. Generate the following architecture:

1. Car Card Component (`src/components/CarCard.tsx`):
- MUST be a named export: `export const CarCard = ({ make, model, year, color, mobile, tablet, desktop }: any) => { ... }`
- Use MUI Card components to display the car data.
- Implement responsive images using MUI's `sx` prop breakpoints. The URLs are passed directly as props.
  - xs (<= 640px): use the `mobile` image URL
  - sm (641px - 1023px): use the `tablet` image URL
  - md (>= 1024px): use the `desktop` image URL

2. Controls Component (`src/components/CarControls.tsx`):
- MUST be a named export: `export const CarControls = ({ onApplySearchSort, addCar }: { onApplySearchSort: (term: string, sort: string) => void, addCar: (car: any) => Promise<void> }) => { ... }`
- Create local state to track draft inputs: `const [draftSearch, setDraftSearch] = useState('')` and `const [draftSort, setDraftSort] = useState('')`.
- Include a text input for search mapped to `draftSearch`.
- Include a dropdown to sort by 'year' or 'make' mapped to `draftSort`.
- Include an "Apply" button. When clicked, it MUST call `onApplySearchSort(draftSearch, draftSort)`.
- Include an "Add Car" button that opens an MUI Dialog.
- The Add Car Dialog MUST EXPLICITLY have EXACTLY 4 TextFields: "Make", "Model", "Year", and "Color". Submit calls `addCar(newCar)` and closes the modal.

TECHNICAL CONSTRAINTS (CRITICAL):
1. DEPENDENCIES: You are STRICTLY FORBIDDEN from using external form libraries. Use native React `useState`.
2. NO LOGIC EDITS: Do not generate or modify `App.tsx` or `useCars.ts`. Only generate the components in `src/components/`.